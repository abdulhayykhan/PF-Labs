#include <iostream>

using namespace std;

int main() {
    int a = 10;
    int b = 20;

    // Swapping values
    swap(a,b);
    // Displaying the swapped values
    cout << "a = " << a << " and b = " << b << endl;

    return 0;
}
